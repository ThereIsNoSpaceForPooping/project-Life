# MCP Server 配置
MCP_SERVER_NAME=project-life-mcp
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8001

# 日志级别
LOG_LEVEL=INFO

# 文件操作根目录（所有 file_* 工具限制在此目录下）
FILE_ROOT_DIR=./files

# 数据库路径
DB_PATH=./data/mcp.db
